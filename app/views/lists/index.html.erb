<h1>The List</h1>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
    <% if list.photo.attached? %>
      <li><%= cl_image_tag list.photo.key, height: 300, width: 400, crop: :fill %></li>
    <% end %>
  <% end %>
</ul>

<%= link_to "Add a Planet", new_list_path %>
